#likes
  -if user_signed_in? && current_user.likes?(@idea)
    =link_to "Unlike", idea_like_path(@idea), method: :delete, remote: true, class: "btn btn-danger"
  -else
    =link_to "Like", idea_like_path(@idea), method: :post, remote: true, class: "btn btn-success"
  =@idea.likes.count
